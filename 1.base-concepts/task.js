"use strict";

function solveEquation(a, b, c) {
  let arr = [];
  let disc = b**2 - 4 * a * c;
  if (disс < 0) { 
    arr = [];
     } else if ( disс == 0) {
      arr = (-b + Math.sqrt(discrim)) / (2*a);
     } else if (disc > 0) {
        arr = [(-b + Math.sqrt(discrim)) / (2*a), (-b - Math.sqrt(discrim)) / (2*a)];  
 }
  return arr; // array
}

function calculateTotalMortgage(percent, contribution, amount, date) {
  let totalAmount;

  // код для задачи №2 писать здесь

  return totalAmount;
}
